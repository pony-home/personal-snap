.form-group.mb-3.col-sm-10
    h3 Finance

       //- TODO: add required 
      .input-group
        span#finance-0-sp.input-group-text.col-sm-8 How do you feel about your financial state right now?
        input#finance-0.form-control(
          aria-describedby="finance-0" 
          type="number"
          min="0"
          max="10"
          name="financeRate"
          placeholder="Score 1 - 10" 
        )

      .input-group
        span#finance-1-sp.input-group-text.col-sm-8 How satisfied you with your income? (Score 1-10)
        input#finance-1.form-control(
          aria-describedby="finance-1" 
          type="text"
        )
        .input-group
        //- rephrase
        span#finance-2-sp.input-group-text.col-sm-8 How much do you earn? (Monthly average)
        input#finance-2.form-control(
          aria-describedby="finance-2" 
          type="text"
        )
        .input-group
        //- rephrase
        span#finance-3-sp.input-group-text.col-sm-8 How much do you spend? (Monthly average)
        input#finance-3.form-control(
          aria-describedby="finance-3" 
          type="text"
        )

        .input-group
        //- rephrase
        span#finance-4-sp.input-group-text.col-sm-8 What percent of your spendings is intended for paying debts? 
        input#finance-4.form-control(
          aria-describedby="finance-4" 
          type="text"
        )
        .input-group
        //- rephrase
        span#finance-5-sp.input-group-text.col-sm-8 What is the percent of passive incomes? (1-100%)
        input#finance-5.form-control(
          aria-describedby="finance-5" 
          type="text"
          placeholder=""
        )

        .input-group
        //- rephrase
        span#finance-6-sp.input-group-text.col-sm-8 How much of your basic needs you can cover with your earnings ? (1-100%)
        input#finance-6.form-control(
          aria-describedby="finance-6" 
          type="text"
          placeholder="Basic needs are: food, place of living, clothes, nothing fancy"
        )

        .input-group
        //- rephrase
        span#finance-7-sp.input-group-text.col-sm-8 How much of your other needs and lusts you can cover with your earnings ? (1-100%)
        input#finance-7.form-control(
          aria-describedby="finance-7" 
          type="text"
        )

        .input-group
        span#finance-8-sp.input-group-text.col-sm-2 Note
        input#finance-8.form-control(
          aria-describedby="finance-8" 
          placeholder="TODO, Current state of things (where do you live (house, rented, etc, what type of vacation/relax/rest can you allow yourself? assess your financial state etc)"
          type="text"
        )


//- TODO add placeholders
//- TODO rephrase all questions
